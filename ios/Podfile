# ملف Podfile مصحح لمشروع Flutter
# هذا الملف يعالج مشاكل شائعة في البناء
# ويدعم إصدارات iOS الحديثة (من 15.0 فما فوق)

# تعيين إصدار منصة iOS إلى 15.0 لضمان التوافق مع أحدث الميزات
platform :ios, '15.0'

# هذا الجزء يحدد المجلدات التي سيتم البحث فيها عن المكتبات (Pods)
target 'Runner' do
  use_frameworks!
  # هذا السطر تم تعديله ليكون متوافقاً مع بيئة البناء
  # لأنه لا يسمح بمسارات المتغيرات
  require_relative '../.ios/Flutter/podhelper.rb'
  
  # هذا الكود هو لحل مشاكل التوقيع (Codesign) الشائعة
  # التي قد تواجهها المكتبات
  # لا تقم بحذفه
  post_install do |installer|
    installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
        config.build_settings['ENABLE_BITCODE'] = 'NO'
        config.build_settings['SWIFT_VERSION'] = '5.0'
      end
    end
  end

  # هذا الجزء يضيف المكتبات التي يحتاجها تطبيقك.
  # يمكنك إضافة المكتبات هنا يدوياً إذا لم يتم إضافتها تلقائياً
  # على سبيل المثال:
  # pod 'some_library'

end
